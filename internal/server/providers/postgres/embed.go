// Package postgres предоставляет реализации инфраструктурных адаптеров,
// репозиториев и кэш-провайдеров для взаимодействия с СУБД PostgreSQL.
package postgres

import "embed"

// migrationsFS инкапсулирует встроенную в бинарный файл виртуальную файловую систему,
// содержащую SQL-скрипты эволюции схем базы данных.
//
// Используется подсистемой Goose на этапе бутстрапа сервера для атомарного наката таблиц.
//
//go:embed migrations/*.sql
var migrationsFS embed.FS
